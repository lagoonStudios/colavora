import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    position: "relative",
    alignItems: "center",
    justifyContent: "center",
    height: 110,
  },
  title: {
    fontWeight: 700,
    fontSize: 20,
  },
  arrowBack: {
    position: "absolute",
    left: 24,
  },
});
