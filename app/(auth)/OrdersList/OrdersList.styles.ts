import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: "100%",
    },
    header: {
        alignItems: "center",
        justifyContent: "center",
        height: 110,
    },
    title: {
        fontWeight: 700,
        fontSize: 20,
    },
    content: {
        alignContent: "center",
        justifyContent: "center",
        paddingHorizontal: 2,
    }
});